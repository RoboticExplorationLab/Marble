# Installing and Rendering Locally
Install `mkdocs-material` via `pip` in a Python environment:
```bash
pip install mkdocs-material
```

From the root of the repository, run:
```bash
mkdocs serve
```

To view the website, visit `http://127.0.0.1:8000`